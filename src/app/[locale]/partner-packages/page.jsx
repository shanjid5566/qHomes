import { PackagesSection } from '@/components/partner';
import React from 'react';

const page = () => {
    return (
        <div>
            <PackagesSection/>
        </div>
    );
};

export default page;