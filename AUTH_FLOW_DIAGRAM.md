# Authentication Flow Diagram

## 🔄 Login Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER LOGIN FLOW                          │
└─────────────────────────────────────────────────────────────────┘

    User enters credentials
           │
           ▼
    ┌──────────────┐
    │ SignInForm   │ ──────────► Form validation
    └──────┬───────┘                  │
           │                          │
           │                     ✅ Valid / ❌ Invalid
           │                          │
           ▼                          │
    ┌──────────────┐                  │
    │ AuthContext  │ ◄────────────────┘
    │  login()     │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │ authService  │ ──────────► POST /api/auth/login
    │  .login()    │                  │
    └──────┬───────┘                  │
           │                          │
           │ ◄────────────────────────┘
           │      Backend Response
           │      {token, user}
           ▼
    ┌──────────────┐
    │ Role Mapping │
    │ user → client│
    │ superAdmin   │
    │   → admin    │
    │ partner      │
    │   → partner  │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │ Store Data   │
    │ - localStorage
    │ - Cookies    │
    └──────┬───────┘
           │
           ▼
    ┌──────────────────────────────┐
    │ Redirect to Role Dashboard   │
    │ client  → /dashboard/client  │
    │ admin   → /dashboard/admin   │
    │ partner → /dashboard/partner │
    └──────────────────────────────┘
```

---

## 🆕 Registration Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      USER REGISTRATION FLOW                      │
└─────────────────────────────────────────────────────────────────┘

    User fills registration form
           │
           ▼
    ┌──────────────┐
    │ RegisterForm │ ──────────► Form validation
    └──────┬───────┘             - Name
           │                     - Email
           │                     - Password match
           │                     - Terms accepted
           ▼
    ┌──────────────┐
    │ AuthContext  │
    │  register()  │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │ authService  │ ──────────► POST /api/auth/register
    │  .register() │             { name, email, password, role: 'user' }
    └──────┬───────┘                  │
           │                          │
           │ ◄────────────────────────┘
           │      Backend Response
           │      {token, user}
           ▼
    ┌──────────────┐
    │ Store Data & │
    │ Auto Login   │
    └──────┬───────┘
           │
           ▼
    ┌──────────────────────────────┐
    │ Redirect to Client Dashboard │
    │ (New users are 'client')     │
    └──────────────────────────────┘
```

---

## 🔒 Protected Route Access

```
┌─────────────────────────────────────────────────────────────────┐
│                      API REQUEST WITH AUTH                       │
└─────────────────────────────────────────────────────────────────┘

    Component makes API call
           │
           ▼
    ┌──────────────┐
    │ api.get()    │
    │ api.post()   │
    │ etc.         │
    └──────┬───────┘
           │
           ▼
    ┌──────────────────────────────┐
    │ Axios Request Interceptor    │
    │ ───────────────────────────  │
    │ 1. Read token from localStorage
    │ 2. Attach to headers:        │
    │    Authorization: Bearer {token}
    └──────┬───────────────────────┘
           │
           ▼
    ┌──────────────┐
    │ Backend API  │
    └──────┬───────┘
           │
           ▼
    ┌──────────────────────────────┐
    │ Response Interceptor         │
    │ ───────────────────────────  │
    │ ✅ 200 → Return data         │
    │ ❌ 401 → Clear auth & redirect
    │ ❌ 403 → Access denied       │
    │ ❌ 500 → Server error        │
    └──────┬───────────────────────┘
           │
           ▼
    Component receives data
```

---

## 🚪 Logout Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                          LOGOUT FLOW                             │
└─────────────────────────────────────────────────────────────────┘

    User clicks logout
           │
           ▼
    ┌──────────────┐
    │ AuthContext  │
    │  logout()    │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │ authService  │ ──────────► POST /api/auth/logout (optional)
    │  .logout()   │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │ Clear Data   │
    │ - localStorage
    │ - Cookies    │
    └──────┬───────┘
           │
           ▼
    ┌──────────────────────────────┐
    │ Redirect to Login Page       │
    │ /[locale]/login              │
    └──────────────────────────────┘
```

---

## 🗂️ File Structure & Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     FILE STRUCTURE OVERVIEW                      │
└─────────────────────────────────────────────────────────────────┘

src/
├── lib/
│   ├── axios.js ──────────┐
│   │  - Base config       │
│   │  - Interceptors      │
│   │  - Error handling    │
│   │                      │
│   └── api.js ────────────┤
│       - get()            │
│       - post()           │
│       - put()            │
│       - delete()         │
│       - uploadFile()     │
│                          │
├── services/              │
│   └── authService.js ────┤
│       - login()          │
│       - register()       │
│       - logout()         │
│       - getCurrentUser() │
│                          │
├── contexts/              │
│   └── AuthContext.jsx ───┤
│       - State management │
│       - Role mapping     │
│       - Redirects        │
│                          │
└── components/            │
    └── auth/              │
        ├── SignInForm.jsx ─┤
        │   - Form UI       │
        │   - Validation    │
        │                   │
        └── RegisterForm.jsx┤
            - Form UI       │
            - Validation    │
                            │
            ┌───────────────┘
            │
            ▼
    ┌─────────────────┐
    │   Backend API   │
    │                 │
    │ Endpoints:      │
    │ - POST /login   │
    │ - POST /register│
    │ - POST /logout  │
    │ - GET  /me      │
    └─────────────────┘
```

---

## 🎯 Role Mapping System

```
┌─────────────────────────────────────────────────────────────────┐
│                       ROLE MAPPING LOGIC                         │
└─────────────────────────────────────────────────────────────────┘

Backend Response        Frontend Mapping         Dashboard Route
─────────────────       ────────────────         ───────────────
role: "user"       →    role: "client"      →    /en/dashboard/client
role: "superAdmin" →    role: "admin"       →    /en/dashboard/admin
role: "partner"    →    role: "partner"     →    /en/dashboard/partner


Implementation in AuthContext.jsx:
──────────────────────────────────

const mapRoleToFrontend = (backendRole) => {
  const roleMapping = {
    user: 'client',
    superAdmin: 'admin',
    partner: 'partner',
  };
  return roleMapping[backendRole] || 'client';
};

const dashboardRoutes = {
  admin: `/${locale}/dashboard/admin`,
  client: `/${locale}/dashboard/client`,
  partner: `/${locale}/dashboard/partner`,
};

router.push(dashboardRoutes[frontendRole]);
```

---

## 🔐 Token Storage & Usage

```
┌─────────────────────────────────────────────────────────────────┐
│                    TOKEN STORAGE STRATEGY                        │
└─────────────────────────────────────────────────────────────────┘

After Login:
────────────

localStorage.setItem('user', JSON.stringify({
  id: "123",
  email: "user@example.com",
  name: "User Name",
  role: "client",
  token: "eyJhbGc..." ← JWT Token stored here
}));

document.cookie = "user={...}; path=/; max-age=86400";


On Every API Request:
─────────────────────

1. Axios interceptor reads localStorage
2. Extracts token from user object
3. Adds to headers:
   Authorization: Bearer eyJhbGc...


On 401 Unauthorized:
────────────────────

1. Clear localStorage
2. Clear cookies
3. Redirect to /login
```

---

## 📊 State Management Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    AUTH CONTEXT STATE FLOW                       │
└─────────────────────────────────────────────────────────────────┘

Initial State:
─────────────
{
  user: null,
  loading: true,
  isAuthenticated: false
}

After Login Success:
───────────────────
{
  user: {
    id: "123",
    email: "user@example.com",
    name: "User Name",
    role: "client",
    backendRole: "user",
    token: "jwt-token"
  },
  loading: false,
  isAuthenticated: true
}

After Logout:
────────────
{
  user: null,
  loading: false,
  isAuthenticated: false
}


Context Provides:
────────────────
- user           → Current user object
- login()        → Login function
- logout()       → Logout function
- register()     → Registration function
- hasRole()      → Check user role
- loading        → Loading state
- isAuthenticated → Boolean auth status
```

This visual guide helps understand the complete authentication flow! 🎉
